<?xml version="1.0" encoding="UTF-8"?>
<project name="adito-application-vnc" default="dist">

        <!-- Standard build locations -->
        <property name="build.src" value="src" />
        <property name="output.file" value="adito-application-vnc.zip" />

        <!-- Build the adito-application-vnc.zip -->
        <target name="dist">
                <zip destfile="${output.file}">
                        <zipfileset dir="${build.src}" prefix="adito-application-vnc">
                        </zipfileset>
                </zip>
        </target>

</project>

